const {expect} = require('chai');
var Instances = require('../Calculator')
describe('My Calculator Testing', () => {
    it('Call Calculator', () => {
        var Instance = new Instances()
        expect(Instance).to.equal(True) // expected true to equal false
    
    })

})   
    
